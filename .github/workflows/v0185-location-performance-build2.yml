name: v0.18.5 Location Performance Build 2

on:
  push:
    branches: [chatgpt/v0.18.5-location-performance]
    paths:
      - '.v0185-location-performance-build2-trigger'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

concurrency:
  group: v0185-location-performance-build2
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/v0.18.5-location-performance
          fetch-depth: 0

      - name: Apply nullable camera fix and commit
        shell: bash
        run: |
          set -euo pipefail
          python .github/scripts/fix-v0185-camera-null.py
          git config user.name 'NV Build Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add app/src/main/java/ir/nv/navigation/map/OnlineIranMap.kt
          git diff --cached --check
          git commit -m 'Fix nullable MapLibre camera target'
          git push origin HEAD:chatgpt/v0.18.5-location-performance

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Install Android 16 SDK
        run: sdkmanager 'platforms;android-36' 'build-tools;36.0.0'

      - name: Lint, unit tests and release build
        env:
          NV_TRAFFIC_API_KEY: ${{ secrets.NV_TRAFFIC_API_KEY }}
          NV_CODE_REGISTRY_URL: ${{ secrets.NV_CODE_REGISTRY_URL }}
        run: |
          gradle --no-daemon \
            :app:lintDebug \
            :app:testDebugUnitTest \
            :app:testReleaseUnitTest \
            :app:assembleDebugAndroidTest \
            :app:assembleRelease

      - name: Sign and verify installable APK
        shell: bash
        run: |
          set -euo pipefail
          TOOLS="$ANDROID_SDK_ROOT/build-tools/36.0.0"
          INPUT='app/build/outputs/apk/release/app-release-unsigned.apk'
          OUTPUT='dist/Rahnama-Android16-v0.18.5.apk'
          mkdir -p dist
          test -s "$INPUT"
          "$TOOLS/apksigner" sign \
            --ks keystore/nv-debug.jks \
            --ks-pass pass:nvdebug \
            --ks-key-alias nvdebug \
            --key-pass pass:nvdebug \
            --out "$OUTPUT" "$INPUT"
          "$TOOLS/apksigner" verify --verbose --print-certs "$OUTPUT" | tee dist/apksigner.txt
          "$TOOLS/zipalign" -c -P 16 -v 4 "$OUTPUT" | tee dist/zipalign.txt
          "$TOOLS/aapt" dump badging "$OUTPUT" | tee dist/badging.txt
          grep -q "package: name='ir.nv.navigation'" dist/badging.txt
          grep -q "versionCode='24'" dist/badging.txt
          grep -q "versionName='0.18.5'" dist/badging.txt
          grep -q "targetSdkVersion:'36'" dist/badging.txt
          sha256sum "$OUTPUT" | tee dist/sha256.txt

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Android 16 install, GPS and product-flow tests
        uses: reactivecircus/android-emulator-runner@v2
        env:
          NV_VERIFY_APK: dist/Rahnama-Android16-v0.18.5.apk
          NV_VERIFY_PACKAGE: ir.nv.navigation
          NV_VERIFY_ACTIVITY: ir.nv.navigation.MainActivity
          NV_TRAFFIC_API_KEY: ${{ secrets.NV_TRAFFIC_API_KEY }}
          NV_CODE_REGISTRY_URL: ${{ secrets.NV_CODE_REGISTRY_URL }}
        with:
          api-level: 36
          target: default
          arch: x86_64
          profile: pixel_2
          cores: 2
          disable-animations: true
          emulator-boot-timeout: 600
          emulator-options: -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim -no-metrics
          script: bash .github/scripts/verify-nv-launch-modern-v3.sh && gradle --no-daemon :app:connectedDebugAndroidTest

      - name: Upload installable v0.18.5 APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Rahnama-Android16-v0.18.5-location-performance-final
          path: |
            dist/Rahnama-Android16-v0.18.5.apk
            dist/apksigner.txt
            dist/zipalign.txt
            dist/badging.txt
            dist/sha256.txt
          retention-days: 30

      - name: Upload diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: Rahnama-v0.18.5-build2-failure-diagnostics
          path: |
            nv-modern-*.txt
            nv-modern-*.png
            nv-modern-*.xml
            app/build/reports/**
          if-no-files-found: ignore
          retention-days: 14
